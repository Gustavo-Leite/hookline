root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/worker ./cmd/worker"
  entrypoint = ["./tmp/worker"]
  include_ext = ["go"]
  exclude_dir = ["tmp", "migrations", ".git", ".github"]
  exclude_regex = ["_test\\.go"]
  delay = 200
  stop_on_error = true

[misc]
  clean_on_exit = true
